<div class="container">
  <div class="row">
    <div class="col-12">
      <header class="page-header" role="heading">
        <h1 class="page-title">{{ page.title }}</h1>
      </header>
    </div>
  </div>
  <div class="rte">
    {{ page.content }}
  </div>
</div>
